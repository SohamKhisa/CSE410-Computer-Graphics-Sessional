# To install OpenGL and GLUT:

$ sudo apt-get update
$ sudo apt-get install libglu1-mesa-dev freeglut3-dev mesa-common-dev


To Compile the Program

$ g++ 1.cpp -o code1 -lglut -lGLU -lGL

To run:
./code1
